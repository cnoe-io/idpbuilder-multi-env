apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: potatohead-deploy
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchLabels:
          workload-class: "app-runtime"
  template:
    metadata:
      name: potatohead-{{index .metadata.labels "environment-name"}}
      namespace: argocd
    spec:
      destination:
        server: '{{.server}}'
        namespace: potatohead
      source:
        repoURL: cnoe://../../
        targetRevision: HEAD
        # with path set to '.' and cnoe://manifests. we are wanting ArgoCD to sync from the ./manifests directory.
        path: workloads/keptn-potatohead
      project: default
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true